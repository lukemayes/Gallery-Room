describe("Gallery", function() {
    it("Each artist is asked for.", function() {
        console.log(this);

        // gallery.httpGet = function(path, data_handler) {
        //     return JSON.stringify(["Mayes", "Barry"]);
        // };

        // var artists;
        // gallery.service.load(function(artists) {
        //     this.artists = artists;
        // });

        // console.log(artists);
    });
});